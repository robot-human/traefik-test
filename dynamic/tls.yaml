tls:
  certResolver: "letsencrypt"
  options: "modern-tls"
  domains:
    - main: "paninimania.cloud"
      sans:
        - "www.paninimania.cloud"
        - "api.paninimania.cloud"
  certificates:
    - certFile: /certs/local.crt
      keyFile:  /certs/local.key